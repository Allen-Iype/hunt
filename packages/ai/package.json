{
  "name": "@hunt/ai",
  "version": "0.0.1",
  "private": true,
  "description": "Hunt AI gateway: named structured tasks over pluggable LLM providers, with caching and record/replay.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hunt/core": "workspace:*",
    "zod": "^4.0.0"
  }
}
